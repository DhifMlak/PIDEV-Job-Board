@model JobsForCoders.Models.Report

<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />

<h2>Hello, @Model.Entreprise.Name</h2>

<p>Here is a list of your recent applicants.</p>

<table class="table">
    <tr>
        <th>
            Position
        </th>
        <th>
            Applicant Name
        </th>
        <th>
            Applicant Surname
        <th>
            Application Date
        </th>
    </tr>

    @foreach (var item in Model.Applications)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Offre.Position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Candidat.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Candidat.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Application_Date)
            </td>
        </tr>
    }
</table>